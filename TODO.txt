1. User Regions to be serialized to cache as a proper array
   System basically working
   Claim Name constants for all APIs
   Merge to master

2. Stay on feature branchh
   Learn how to use System.Text.Json via new JsonDemo class and debug output
   Update to 5.1.0 of IdentityModel library
   Caching of JWKS keys

3. Claims Principal handling is naff
   JWT validation should ideally not use a claims principal
   CustomAuthenticationManager should populate it properly
